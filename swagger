swagger: '2.0'
info:
  version: 1.0.0.0
  title: llm-infra-deploy
  description: swagger definition for LLM
host: llm-infra-services.jpmchase.net
basePath: /api
schemes:
  - https
consumes:
  - application/json
produces:
  - application/json
paths:
  /infradeploy:
    post:
      description: deployment of LLM resources.
      operationId: infraservices-invoke
      parameters:
        - name: api-version
          in: query
          type: string
          enum: [2022-05-01]
          description: The service API version.
          required: true
        - name: body
          in: body
          description: The request body.
          required: false
          schema:
            $ref: '#/definitions/request-manual'
      responses:
        '200':
          description: The Response for action with status code '200'.
          schema:
            $ref: '#/definitions/response-200'
        default:
          description: llm-infra-deploy response
          schema:
            type: object
      deprecated: false
definitions:
  response-200:
    properties:
      deployment_name:
        type: string
      resource_group_name:
        type: string
      status:
        type: string
      subscription_id:
        type: string
    type: object
  request-manual:
    properties:
      aad_groups:
        type: string
      location:
        type: string
      subscription_id:
        type: string
      workspace_id:
        type: string
      workspace_type:
        type: string
    type: object
